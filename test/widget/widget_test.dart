import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
      'Given response is not empty after calling fetchGlucoseList then UI shoudl give widget with key should return List<Glucose> ',
      (WidgetTester tester) async {});

  testWidgets(
      'Given response is empty after calling fetchGlucoseList then return empty list ',
      (WidgetTester tester) async {});
}
